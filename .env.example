# Frontend (Vite) — opcional
# VITE_* só afeta o build do cliente.

# Firebase Functions (configurar com `firebase functions:config:set` ou secrets)
# URL HTTP POST para envio de WhatsApp (webhook do teu provedor).
# Corpo JSON: { to, phone, message, text, body }
# WHATSAPP_SEND_URL=
# WHATSAPP_SEND_TOKEN=
